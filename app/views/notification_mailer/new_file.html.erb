<%= simple_format @message.message %>

<%= link_to admin_conference_url(:id => @message.url_id, :locale => @message.locale),
						admin_conference_url(:id => @message.url_id, :locale => @message.locale) %>
